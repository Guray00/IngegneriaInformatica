#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#include <ctype.h>

int string_to_int(char*);
char* timestamp();
char* remaining_time(int);
void read_from_stdin(char*);
void substr(const char*, char*, int, int);
void trim(char*);
